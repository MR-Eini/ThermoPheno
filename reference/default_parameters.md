# Default ThermoPheno crop parameters

Default ThermoPheno crop parameters

## Usage

``` r
default_parameters(crop_type = c("summer", "winter"))
```

## Arguments

- crop_type:

  `summer` or `winter`.

## Value

A named list of default crop parameters.
