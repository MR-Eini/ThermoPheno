Upload these files to the root of the MR-Eini/ThermoPheno repository:

1. README.md
   - Replaces the homepage content used by pkgdown.
   - Fixes Markdown/table rendering and removes outdated repository text.

2. DESCRIPTION
   - Fixes package metadata, author parsing, dependency placement, and repository URLs.
   - Uses the production repository URL, not ThermoPheno-dev.

3. _pkgdown.yml
   - Fixes the package website URL and GitHub link.
   - Ensures the homepage and reference index point to the production ThermoPheno site.

After uploading, run in R:

devtools::document()
devtools::test()
rcmdcheck::rcmdcheck(args = c("--no-manual", "--as-cran"))
pkgdown::build_site()

Then push the generated pkgdown site or allow the GitHub Actions pkgdown workflow to rebuild it.


Correction in this version:
- README.md now includes the corrected repository structure, including .github/, tests/, validation/, vignettes/, NEWS.md, RUN_CHECKS.R, SOURCE_PACKAGE_NOTES.md, and man/figures/.
